const eventHub = document.querySelector('.container');
const contentTarget = document.querySelector('');

// we need code to make the known associates render to the dom. Sorted by the criminal id. So when we hit the button it checks the id.
// That id will be used to sort the criminal. And pull the right alibis.
